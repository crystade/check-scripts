[
  {
    "groupId": "simple-latency-check-for-gaming",
    "name": "Simple Latency Check for Gaming Servers",
    "description": "A RTT around 60ms is considered acceptable for most online gaming servers\n",
    "variants": [
      {
        "variantId": "simple-latency-check-for-gaming:alerting-all",
        "name": "Alert to all channels if exceeds 60ms RTT",
        "script": "if context.error == null \u0026\u0026 context.metadata.rttMs \u003e 60 {\r\n    alerting.send({\r\n        header: \"Monitor [%s] notices high RTT from %s\"\r\n            .format(context.monitor.name, context.location),\r\n        message: \"Measured a RTT of %.2fms\\nCheck ID: %s\"\r\n            .format(context.metadata.rttMs, context.check.id),\r\n        footer: \"%s • Monitored by Crystade\"\r\n            .format(context.team.name),\r\n        deduplication: {\r\n            key: context.monitor.id + \":simple-uptime-check\",\r\n            ttl: \"1h\"\r\n        }\r\n    });\r\n}\r\n",
        "checksum": "9acc4ca57f9f675247f15311795a48a97d45a9e0b78e67dc198dff8ca72f1cd8"
      },
      {
        "variantId": "simple-latency-check-for-gaming:minor-incident",
        "name": "Report minor incident if exceeds 60ms RTT",
        "script": "if context.error == null {\r\n    if context.metadata.rttMs \u003c= 60 {\r\n        incident.recover();\r\n    } else {\r\n        incident.signal({\r\n            severity: \"minor\",\r\n            title: \"Monitor [%s] notices high RTT from %s\".format(context.monitor.name, context.location),\r\n            content: \"Measured a RTT of %.2fms\\nCheck ID: %s\".format(context.metadata.rttMs, context.check.id)\r\n        });\r\n    }\r\n}",
        "checksum": "35331d5cec89fbfd83d14898fb34201542ef0055f9129f670ad0ef2ca1e97d88"
      },
      {
        "variantId": "simple-latency-check-for-gaming:major-incident",
        "name": "Report major incident if exceeds 60ms RTT",
        "script": "if context.error == null {\r\n    if context.metadata.rttMs \u003c= 60 {\r\n        incident.recover();\r\n    } else {\r\n        incident.signal({\r\n            severity: \"major\",\r\n            title: \"Monitor [%s] notices high RTT from %s\".format(context.monitor.name, context.location),\r\n            content: \"Measured a RTT of %.2fms\\nCheck ID: %s\".format(context.metadata.rttMs, context.check.id)\r\n        });\r\n    }\r\n}",
        "checksum": "d088ad7f4f6a8f3db421cca9a2b84bf14756552426a5541c47079bd1257388d6"
      },
      {
        "variantId": "simple-latency-check-for-gaming:critical-incident",
        "name": "Report critical incident if exceeds 60ms RTT",
        "script": "if context.error == null {\r\n    if context.metadata.rttMs \u003c= 60 {\r\n        incident.recover();\r\n    } else {\r\n        incident.signal({\r\n            severity: \"critical\",\r\n            title: \"Monitor [%s] notices high RTT from %s\".format(context.monitor.name, context.location),\r\n            content: \"Measured a RTT of %.2fms\\nCheck ID: %s\".format(context.metadata.rttMs, context.check.id)\r\n        });\r\n    }\r\n}",
        "checksum": "4d3e255ad717d1aad108d6cdb6c45645b1987867cc84c4ec5c4644b1eebb3cab"
      }
    ]
  },
  {
    "groupId": "simple-latency-check-for-website",
    "name": "Simple Latency Check for Websites",
    "description": "A RTT around 100ms is considered acceptable for most websites per https://aws.amazon.com/what-is/rtt-in-networking/\n",
    "variants": [
      {
        "variantId": "simple-latency-check-for-website:alerting-all",
        "name": "Alert to all channels if exceeds 100ms RTT",
        "script": "if context.error == null \u0026\u0026 context.metadata.rttMs \u003e 100 {\r\n    alerting.send({\r\n        header: \"Monitor [%s] notices high RTT from %s\"\r\n            .format(context.monitor.name, context.location),\r\n        message: \"Measured a RTT of %.2fms\\nCheck ID: %s\"\r\n            .format(context.metadata.rttMs, context.check.id),\r\n        footer: \"%s • Monitored by Crystade\"\r\n            .format(context.team.name),\r\n        deduplication: {\r\n            key: context.monitor.id + \":simple-uptime-check\",\r\n            ttl: \"1h\"\r\n        }\r\n    });\r\n}\r\n",
        "checksum": "944686fb3205e510f4d0f3f5fa1881667cf728ccfc46d5d19ee7d0011dc22aa7"
      },
      {
        "variantId": "simple-latency-check-for-website:minor-incident",
        "name": "Report minor incident if exceeds 100ms RTT",
        "script": "if context.error == null {\r\n    if context.metadata.rttMs \u003c= 100 {\r\n        incident.recover();\r\n    } else {\r\n        incident.signal({\r\n            severity: \"minor\",\r\n            title: \"Monitor [%s] notices high RTT from %s\".format(context.monitor.name, context.location),\r\n            content: \"Measured a RTT of %.2fms\\nCheck ID: %s\".format(context.metadata.rttMs, context.check.id)\r\n        });\r\n    }\r\n}",
        "checksum": "0cf81bda06f5b14a18118d5c5ae0db95ac1f900bded34b244e6333774e555a86"
      },
      {
        "variantId": "simple-latency-check-for-website:major-incident",
        "name": "Report major incident if exceeds 100ms RTT",
        "script": "if context.error == null {\r\n    if context.metadata.rttMs \u003c= 100 {\r\n        incident.recover();\r\n    } else {\r\n        incident.signal({\r\n            severity: \"major\",\r\n            title: \"Monitor [%s] notices high RTT from %s\".format(context.monitor.name, context.location),\r\n            content: \"Measured a RTT of %.2fms\\nCheck ID: %s\".format(context.metadata.rttMs, context.check.id)\r\n        });\r\n    }\r\n}",
        "checksum": "3be24e3bd1df370a4131cee474d2798a1252308410624234791d0b6eee2f7729"
      },
      {
        "variantId": "simple-latency-check-for-website:critical-incident",
        "name": "Report critical incident if exceeds 100ms RTT",
        "script": "if context.error == null {\r\n    if context.metadata.rttMs \u003c= 100 {\r\n        incident.recover();\r\n    } else {\r\n        incident.signal({\r\n            severity: \"critical\",\r\n            title: \"Monitor [%s] notices high RTT from %s\".format(context.monitor.name, context.location),\r\n            content: \"Measured a RTT of %.2fms\\nCheck ID: %s\".format(context.metadata.rttMs, context.check.id)\r\n        });\r\n    }\r\n}",
        "checksum": "09fc01306e3a112aa9c41a923479e825c6e9ae54f6017cb2bd115cdbc1d5f64d"
      }
    ]
  },
  {
    "groupId": "simple-mcp-check",
    "name": "Simple MCP Check",
    "description": "A simple script to check for MCP server status and capabilities\n",
    "variants": [
      {
        "variantId": "simple-mcp-check:alerting-all",
        "name": "Alert to all channels on error",
        "script": "if context.error != null {\r\n    var mcpServerName = \"unknown\";\r\n    var mcpServerVersion = \"?\";\r\n\r\n    for layer in context.metadata.layers {\r\n        if layer.name == \"mcp\" \u0026\u0026 layer.data != null {\r\n            const srv = layer.data.serverInfo;\r\n            if srv != null {\r\n                mcpServerName = srv.name;\r\n                mcpServerVersion = srv.version;\r\n            }\r\n            break;\r\n        }\r\n    }\r\n\r\n    alerting.send({\r\n        header: \"Monitor [%s] MCP server is down from %s\"\r\n            .format(context.monitor.name, context.location),\r\n        message: \"Got error: %s (code: %s)\\nMCP Server: %s v%s\\nCheck ID: %s\"\r\n            .format(context.error.message, context.error.code,\r\n                mcpServerName, mcpServerVersion, context.check.id),\r\n        footer: \"%s • Monitored by Crystade\"\r\n            .format(context.team.name),\r\n        deduplication: {\r\n            key: context.monitor.id + \":mcp-check\",\r\n            ttl: \"1h\"\r\n        }\r\n    });\r\n}\r\n",
        "checksum": "1c6abcef9e136caf1c1281a91fd5ce548b3921c4a80c13d6f169068aaf8342ce"
      },
      {
        "variantId": "simple-mcp-check:minor-incident",
        "name": "Report minor incident on error",
        "script": "if context.error == null {\r\n    incident.recover();\r\n} else {\r\n    var mcpServerName = \"unknown\";\r\n    var mcpServerVersion = \"?\";\r\n\r\n    for layer in context.metadata.layers {\r\n        if layer.name == \"mcp\" \u0026\u0026 layer.data != null {\r\n            const srv = layer.data.serverInfo;\r\n            if srv != null {\r\n                mcpServerName = srv.name;\r\n                mcpServerVersion = srv.version;\r\n            }\r\n            break;\r\n        }\r\n    }\r\n\r\n    incident.signal({\r\n        severity: \"minor\",\r\n        title: \"Monitor [%s] MCP server is down from %s\"\r\n            .format(context.monitor.name, context.location),\r\n        content: \"Got error: %s (code: %s)\\nMCP Server: %s v%s\\nCheck ID: %s\"\r\n            .format(context.error.message, context.error.code,\r\n                mcpServerName, mcpServerVersion, context.check.id)\r\n    });\r\n}\r\n",
        "checksum": "e3f61e8f009af1b18db15d40ee52e8395366c334a474a293fa6ef6b7eed7eb8e"
      },
      {
        "variantId": "simple-mcp-check:major-incident",
        "name": "Report major incident on error",
        "script": "if context.error == null {\r\n    incident.recover();\r\n} else {\r\n    var mcpServerName = \"unknown\";\r\n    var mcpServerVersion = \"?\";\r\n\r\n    for layer in context.metadata.layers {\r\n        if layer.name == \"mcp\" \u0026\u0026 layer.data != null {\r\n            const srv = layer.data.serverInfo;\r\n            if srv != null {\r\n                mcpServerName = srv.name;\r\n                mcpServerVersion = srv.version;\r\n            }\r\n            break;\r\n        }\r\n    }\r\n\r\n    incident.signal({\r\n        severity: \"major\",\r\n        title: \"Monitor [%s] MCP server is down from %s\"\r\n            .format(context.monitor.name, context.location),\r\n        content: \"Got error: %s (code: %s)\\nMCP Server: %s v%s\\nCheck ID: %s\"\r\n            .format(context.error.message, context.error.code,\r\n                mcpServerName, mcpServerVersion, context.check.id)\r\n    });\r\n}\r\n",
        "checksum": "774c00080615a22fdac90622a844040ac7ae4eef970ff9762f9277965d872910"
      },
      {
        "variantId": "simple-mcp-check:critical-incident",
        "name": "Report critical incident on error",
        "script": "if context.error == null {\r\n    incident.recover();\r\n} else {\r\n    var mcpServerName = \"unknown\";\r\n    var mcpServerVersion = \"?\";\r\n\r\n    for layer in context.metadata.layers {\r\n        if layer.name == \"mcp\" \u0026\u0026 layer.data != null {\r\n            const srv = layer.data.serverInfo;\r\n            if srv != null {\r\n                mcpServerName = srv.name;\r\n                mcpServerVersion = srv.version;\r\n            }\r\n            break;\r\n        }\r\n    }\r\n\r\n    incident.signal({\r\n        severity: \"critical\",\r\n        title: \"Monitor [%s] MCP server is down from %s\"\r\n            .format(context.monitor.name, context.location),\r\n        content: \"Got error: %s (code: %s)\\nMCP Server: %s v%s\\nCheck ID: %s\"\r\n            .format(context.error.message, context.error.code,\r\n                mcpServerName, mcpServerVersion, context.check.id)\r\n    });\r\n}\r\n",
        "checksum": "fd1d9a3e9db12968bdbe4559967dd786dcc3918c58b06b2525ca632ffa715451"
      }
    ]
  },
  {
    "groupId": "simple-monitor-check",
    "name": "Simple Monitor Check",
    "description": "A simple script to check for monitor status\n",
    "variants": [
      {
        "variantId": "simple-monitor-check:alerting-all",
        "name": "Alert to all channels on error",
        "script": "if context.error != null {\r\n    alerting.send({\r\n        header: \"Monitor [%s] is down from %s\"\r\n            .format(context.monitor.name, context.location),\r\n        message: \"Got error: %s (code: %s)\\nCheck ID: %s\"\r\n            .format(context.error.message, context.error.code, context.check.id),\r\n        footer: \"%s • Monitored by Crystade\"\r\n            .format(context.team.name),\r\n        deduplication: {\r\n            key: context.monitor.id + \":simple-uptime-check\",\r\n            ttl: \"1h\"\r\n        }\r\n    });\r\n}\r\n",
        "checksum": "01126804d9ac60ded14942879d0c32bc94321b16dbcc84f83af3c311937622e3"
      },
      {
        "variantId": "simple-monitor-check:minor-incident",
        "name": "Report minor incident on error",
        "script": "if context.error == null {\r\n    incident.recover();\r\n} else {\r\n    incident.signal({\r\n        severity: \"minor\",\r\n        title: \"Monitor [%s] is down from %s\".format(context.monitor.name, context.location),\r\n        content: \"Got error: %s (code: %s)\\nCheck ID: %s\".format(context.error.message, context.error.code, context.check.id)\r\n    });\r\n}\r\n",
        "checksum": "70c2b674bc2d2e4e82091123ce8466ac4e9f86c551b3fc8fc1976a86c9bc0c75"
      },
      {
        "variantId": "simple-monitor-check:major-incident",
        "name": "Report major incident on error",
        "script": "if context.error == null {\r\n    incident.recover();\r\n} else {\r\n    incident.signal({\r\n        severity: \"major\",\r\n        title: \"Monitor [%s] is down from %s\".format(context.monitor.name, context.location),\r\n        content: \"Got error: %s (code: %s)\\nCheck ID: %s\".format(context.error.message, context.error.code, context.check.id)\r\n    });\r\n}\r\n",
        "checksum": "6945ffc3ac15ccf591b3eb3b6f7c91894e85c3d2a4c0357c252e00d30744627b"
      },
      {
        "variantId": "simple-monitor-check:critical-incident",
        "name": "Report critical incident on error",
        "script": "if context.error == null {\r\n    incident.recover();\r\n} else {\r\n    incident.signal({\r\n        severity: \"critical\",\r\n        title: \"Monitor [%s] is down from %s\".format(context.monitor.name, context.location),\r\n        content: \"Got error: %s (code: %s)\\nCheck ID: %s\".format(context.error.message, context.error.code, context.check.id)\r\n    });\r\n}\r\n",
        "checksum": "3b071196134898091b69796c88744a315fdb297cf8205b635fc75d48a3cb6991"
      }
    ]
  },
  {
    "groupId": "simple-tls-check",
    "name": "Simple TLS Certificate Check",
    "description": "A simple script to monitor TLS certificate status, warning early 7 days before expiration\n",
    "variants": [
      {
        "variantId": "simple-tls-check:alerting-all",
        "name": "Alert to all channels on certificate issues",
        "script": "# Priority 1: connection/handshake error\r\nif context.error != null {\r\n    alerting.send({\r\n        header: \"Monitor [%s] TLS connection failed from %s\"\r\n            .format(context.monitor.name, context.location),\r\n        message: \"Got error: %s (code: %s)\\nCheck ID: %s\"\r\n            .format(context.error.message, context.error.code, context.check.id),\r\n        footer: \"%s • Monitored by Crystade\"\r\n            .format(context.team.name),\r\n        deduplication: {\r\n            key: context.monitor.id + \":tls-cert-check\",\r\n            ttl: \"1h\"\r\n        }\r\n    });\r\n} else {\r\n    # Find TLS layer\r\n    var tlsLayer = null;\r\n    for layer in context.metadata.layers {\r\n        if layer.name == \"tls\" {\r\n            tlsLayer = layer;\r\n            break;\r\n        }\r\n    }\r\n\r\n    if tlsLayer != null \u0026\u0026 tlsLayer.data != null {\r\n        const tlsData = tlsLayer.data;\r\n        const certChain = tlsData.certChain;\r\n\r\n        if certChain != null \u0026\u0026 len(certChain) \u003e 0 {\r\n            const leaf = certChain[0];\r\n            const certSubject = leaf.subject;\r\n            const certIssuer = leaf.issuer;\r\n            const notAfterStr = leaf.notAfter;\r\n\r\n            # Priority 2: check for already-detected cert status issues\r\n            var statusIssues = \"\";\r\n            const statuses = leaf.status;\r\n            if statuses != null {\r\n                for s in statuses {\r\n                    if s != \"OK\" {\r\n                        statusIssues = statusIssues + s + \"; \";\r\n                    }\r\n                }\r\n            }\r\n\r\n            if statusIssues != \"\" {\r\n                alerting.send({\r\n                    header: \"TLS certificate issue for [%s] from %s\"\r\n                        .format(context.monitor.name, context.location),\r\n                    message: \"Certificate: %s\\nIssuer: %s\\nExpires: %s\\nIssues: %s\\nCheck ID: %s\"\r\n                        .format(certSubject, certIssuer, notAfterStr,\r\n                            statusIssues, context.check.id),\r\n                    footer: \"%s • Monitored by Crystade\"\r\n                        .format(context.team.name),\r\n                    deduplication: {\r\n                        key: context.monitor.id + \":tls-cert-check\",\r\n                        ttl: \"1h\"\r\n                    }\r\n                });\r\n            } else {\r\n                # Priority 3: all statuses OK — check 7-day early expiration\r\n                const expiryMs = datetime.parse(notAfterStr);\r\n                const nowMs = datetime.now();\r\n\r\n                if expiryMs - nowMs \u003c duration.days(7) {\r\n                    const daysRemaining = int((expiryMs - nowMs) / 86400000);\r\n                    var expiryLabel = \"EXPIRED\";\r\n                    if daysRemaining \u003e 0 {\r\n                        expiryLabel = \"%d days remaining\".format(daysRemaining);\r\n                    }\r\n\r\n                    alerting.send({\r\n                        header: \"TLS certificate expiring soon for [%s] from %s\"\r\n                            .format(context.monitor.name, context.location),\r\n                        message: \"Certificate: %s\\nIssuer: %s\\nExpires: %s (%s)\\nCheck ID: %s\"\r\n                            .format(certSubject, certIssuer, notAfterStr,\r\n                                expiryLabel, context.check.id),\r\n                        footer: \"%s • Monitored by Crystade\"\r\n                            .format(context.team.name),\r\n                        deduplication: {\r\n                            key: context.monitor.id + \":tls-cert-check\",\r\n                            ttl: \"1h\"\r\n                        }\r\n                    });\r\n                }\r\n            }\r\n        }\r\n    }\r\n}\r\n",
        "checksum": "91706a7fb1d93f8882427ec65b0537f42f37272eed55e868da56f3eb69c60a3c"
      },
      {
        "variantId": "simple-tls-check:minor-incident",
        "name": "Report minor incident on certificate issues",
        "script": "# Priority 1: connection/handshake error\r\nif context.error != null {\r\n    incident.signal({\r\n        severity: \"minor\",\r\n        title: \"Monitor [%s] TLS connection failed from %s\"\r\n            .format(context.monitor.name, context.location),\r\n        content: \"Got error: %s (code: %s)\\nCheck ID: %s\"\r\n            .format(context.error.message, context.error.code, context.check.id)\r\n    });\r\n} else {\r\n    # Find TLS layer\r\n    var tlsLayer = null;\r\n    for layer in context.metadata.layers {\r\n        if layer.name == \"tls\" {\r\n            tlsLayer = layer;\r\n            break;\r\n        }\r\n    }\r\n\r\n    if tlsLayer != null \u0026\u0026 tlsLayer.data != null {\r\n        const tlsData = tlsLayer.data;\r\n        const certChain = tlsData.certChain;\r\n\r\n        if certChain != null \u0026\u0026 len(certChain) \u003e 0 {\r\n            const leaf = certChain[0];\r\n            const certSubject = leaf.subject;\r\n            const certIssuer = leaf.issuer;\r\n            const notAfterStr = leaf.notAfter;\r\n\r\n            # Priority 2: check for already-detected cert status issues\r\n            var statusIssues = \"\";\r\n            const statuses = leaf.status;\r\n            if statuses != null {\r\n                for s in statuses {\r\n                    if s != \"OK\" {\r\n                        statusIssues = statusIssues + s + \"; \";\r\n                    }\r\n                }\r\n            }\r\n\r\n            if statusIssues != \"\" {\r\n                incident.signal({\r\n                    severity: \"minor\",\r\n                    title: \"TLS certificate issue for [%s] from %s\"\r\n                        .format(context.monitor.name, context.location),\r\n                    content: \"Certificate: %s\\nIssuer: %s\\nExpires: %s\\nIssues: %s\\nCheck ID: %s\"\r\n                        .format(certSubject, certIssuer, notAfterStr,\r\n                            statusIssues, context.check.id)\r\n                });\r\n            } else {\r\n                # Priority 3: all statuses OK — check 7-day early expiration\r\n                const expiryMs = datetime.parse(notAfterStr);\r\n                const nowMs = datetime.now();\r\n\r\n                if expiryMs - nowMs \u003c duration.days(7) {\r\n                    const daysRemaining = int((expiryMs - nowMs) / 86400000);\r\n                    var expiryLabel = \"EXPIRED\";\r\n                    if daysRemaining \u003e 0 {\r\n                        expiryLabel = \"%d days remaining\".format(daysRemaining);\r\n                    }\r\n\r\n                    incident.signal({\r\n                        severity: \"minor\",\r\n                        title: \"TLS certificate expiring soon for [%s] from %s\"\r\n                            .format(context.monitor.name, context.location),\r\n                        content: \"Certificate: %s\\nIssuer: %s\\nExpires: %s (%s)\\nCheck ID: %s\"\r\n                            .format(certSubject, certIssuer, notAfterStr,\r\n                                expiryLabel, context.check.id)\r\n                    });\r\n                }\r\n            }\r\n        }\r\n    }\r\n}\r\n",
        "checksum": "8b4cf2e4a4b6d32ba6f1ab0fe8a34340e6102877cb75b6c751cd0658a6ca72c7"
      },
      {
        "variantId": "simple-tls-check:major-incident",
        "name": "Report major incident on certificate issues",
        "script": "# Priority 1: connection/handshake error\r\nif context.error != null {\r\n    incident.signal({\r\n        severity: \"major\",\r\n        title: \"Monitor [%s] TLS connection failed from %s\"\r\n            .format(context.monitor.name, context.location),\r\n        content: \"Got error: %s (code: %s)\\nCheck ID: %s\"\r\n            .format(context.error.message, context.error.code, context.check.id)\r\n    });\r\n} else {\r\n    # Find TLS layer\r\n    var tlsLayer = null;\r\n    for layer in context.metadata.layers {\r\n        if layer.name == \"tls\" {\r\n            tlsLayer = layer;\r\n            break;\r\n        }\r\n    }\r\n\r\n    if tlsLayer != null \u0026\u0026 tlsLayer.data != null {\r\n        const tlsData = tlsLayer.data;\r\n        const certChain = tlsData.certChain;\r\n\r\n        if certChain != null \u0026\u0026 len(certChain) \u003e 0 {\r\n            const leaf = certChain[0];\r\n            const certSubject = leaf.subject;\r\n            const certIssuer = leaf.issuer;\r\n            const notAfterStr = leaf.notAfter;\r\n\r\n            # Priority 2: check for already-detected cert status issues\r\n            var statusIssues = \"\";\r\n            const statuses = leaf.status;\r\n            if statuses != null {\r\n                for s in statuses {\r\n                    if s != \"OK\" {\r\n                        statusIssues = statusIssues + s + \"; \";\r\n                    }\r\n                }\r\n            }\r\n\r\n            if statusIssues != \"\" {\r\n                incident.signal({\r\n                    severity: \"major\",\r\n                    title: \"TLS certificate issue for [%s] from %s\"\r\n                        .format(context.monitor.name, context.location),\r\n                    content: \"Certificate: %s\\nIssuer: %s\\nExpires: %s\\nIssues: %s\\nCheck ID: %s\"\r\n                        .format(certSubject, certIssuer, notAfterStr,\r\n                            statusIssues, context.check.id)\r\n                });\r\n            } else {\r\n                # Priority 3: all statuses OK — check 7-day early expiration\r\n                const expiryMs = datetime.parse(notAfterStr);\r\n                const nowMs = datetime.now();\r\n\r\n                if expiryMs - nowMs \u003c duration.days(7) {\r\n                    const daysRemaining = int((expiryMs - nowMs) / 86400000);\r\n                    var expiryLabel = \"EXPIRED\";\r\n                    if daysRemaining \u003e 0 {\r\n                        expiryLabel = \"%d days remaining\".format(daysRemaining);\r\n                    }\r\n\r\n                    incident.signal({\r\n                        severity: \"major\",\r\n                        title: \"TLS certificate expiring soon for [%s] from %s\"\r\n                            .format(context.monitor.name, context.location),\r\n                        content: \"Certificate: %s\\nIssuer: %s\\nExpires: %s (%s)\\nCheck ID: %s\"\r\n                            .format(certSubject, certIssuer, notAfterStr,\r\n                                expiryLabel, context.check.id)\r\n                    });\r\n                }\r\n            }\r\n        }\r\n    }\r\n}\r\n",
        "checksum": "71dc91eb6eda986ec3a7278ae3ed4ec3ca8ff0c26ecdd3ff33887233794bbc1b"
      },
      {
        "variantId": "simple-tls-check:critical-incident",
        "name": "Report critical incident on certificate issues",
        "script": "# Priority 1: connection/handshake error\r\nif context.error != null {\r\n    incident.signal({\r\n        severity: \"critical\",\r\n        title: \"Monitor [%s] TLS connection failed from %s\"\r\n            .format(context.monitor.name, context.location),\r\n        content: \"Got error: %s (code: %s)\\nCheck ID: %s\"\r\n            .format(context.error.message, context.error.code, context.check.id)\r\n    });\r\n} else {\r\n    # Find TLS layer\r\n    var tlsLayer = null;\r\n    for layer in context.metadata.layers {\r\n        if layer.name == \"tls\" {\r\n            tlsLayer = layer;\r\n            break;\r\n        }\r\n    }\r\n\r\n    if tlsLayer != null \u0026\u0026 tlsLayer.data != null {\r\n        const tlsData = tlsLayer.data;\r\n        const certChain = tlsData.certChain;\r\n\r\n        if certChain != null \u0026\u0026 len(certChain) \u003e 0 {\r\n            const leaf = certChain[0];\r\n            const certSubject = leaf.subject;\r\n            const certIssuer = leaf.issuer;\r\n            const notAfterStr = leaf.notAfter;\r\n\r\n            # Priority 2: check for already-detected cert status issues\r\n            var statusIssues = \"\";\r\n            const statuses = leaf.status;\r\n            if statuses != null {\r\n                for s in statuses {\r\n                    if s != \"OK\" {\r\n                        statusIssues = statusIssues + s + \"; \";\r\n                    }\r\n                }\r\n            }\r\n\r\n            if statusIssues != \"\" {\r\n                incident.signal({\r\n                    severity: \"critical\",\r\n                    title: \"TLS certificate issue for [%s] from %s\"\r\n                        .format(context.monitor.name, context.location),\r\n                    content: \"Certificate: %s\\nIssuer: %s\\nExpires: %s\\nIssues: %s\\nCheck ID: %s\"\r\n                        .format(certSubject, certIssuer, notAfterStr,\r\n                            statusIssues, context.check.id)\r\n                });\r\n            } else {\r\n                # Priority 3: all statuses OK — check 7-day early expiration\r\n                const expiryMs = datetime.parse(notAfterStr);\r\n                const nowMs = datetime.now();\r\n\r\n                if expiryMs - nowMs \u003c duration.days(7) {\r\n                    const daysRemaining = int((expiryMs - nowMs) / 86400000);\r\n                    var expiryLabel = \"EXPIRED\";\r\n                    if daysRemaining \u003e 0 {\r\n                        expiryLabel = \"%d days remaining\".format(daysRemaining);\r\n                    }\r\n\r\n                    incident.signal({\r\n                        severity: \"critical\",\r\n                        title: \"TLS certificate expiring soon for [%s] from %s\"\r\n                            .format(context.monitor.name, context.location),\r\n                        content: \"Certificate: %s\\nIssuer: %s\\nExpires: %s (%s)\\nCheck ID: %s\"\r\n                            .format(certSubject, certIssuer, notAfterStr,\r\n                                expiryLabel, context.check.id)\r\n                    });\r\n                }\r\n            }\r\n        }\r\n    }\r\n}\r\n",
        "checksum": "6d8f81309d5f6fcda2a2a9bba82f20aed0d6ab896a1304c6533040a01c390a51"
      }
    ]
  }
]
