{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Template",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "name",
    "variants"
  ],
  "properties": {
    "groupId": {
      "type": "string",
      "description": "The template group id."
    },
    "name": {
      "type": "string",
      "description": "The template group name."
    },
    "description": {
      "type": "string",
      "description": "The template group description."
    },
    "variants": {
      "type": "array",
      "description": "The list of template variants.",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/variant"
      }
    }
  },
  "$defs": {
    "variant": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "variantId",
        "name",
        "scriptFile"
      ],
      "properties": {
        "variantId": {
          "type": "string",
          "description": "The variant id."
        },
        "name": {
          "type": "string",
          "description": "The variant name."
        },
        "description": {
          "type": "string",
          "description": "The variant description."
        },
        "scriptFile": {
          "type": "string",
          "description": "The path to the script file."
        }
      }
    }
  }
}